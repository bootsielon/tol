#
# This file is a Makefile for Tolbase TCL Extension.  If it has the name
# "Makefile.in" then it is a template for a Makefile;  to generate the actual
# Makefile, run "./configure", which is a configuration script generated by the
# "autoconf" program (constructs like "@foo@" will get replaced in the actual
# Makefile.
#

#========================================================================
# Edit the following few lines when writing a new extension
#========================================================================

TESTS_FILES	= $(srcdir)/tests/*.tol

DIST_FILES	= $(srcdir)/COPYING $(srcdir)/INSTALL $(srcdir)/NEWS \
	$(srcdir)/README $(srcdir)/THANKS $(srcdir)/TODO $(srcdir)/ChangeLog \
	$(srcdir)/lib/tolbase.sh

TTK_SCRIPTS = $(srcdir)/lib/toltk/*.tcl $(srcdir)/lib/toltk/*.tol \
	$(srcdir)/lib/toltk/*.ini $(srcdir)/lib/toltk/*.rc \
	$(srcdir)/lib/toltk/tclIndex

TTK_LANG_FILES = $(srcdir)/lib/toltk/msgs/*.msg

TTK_IMAGE_FILES = $(srcdir)/lib/toltk/images/*.gif \
	$(srcdir)/lib/toltk/images/*.ico $(srcdir)/lib/toltk/images/*.xbm \
	$(srcdir)/lib/toltk/images/*.cur

BYW_SCRIPTS = $(srcdir)/lib/byswidget/*.tcl
BYW_BARIMA_SCRIPTS = $(srcdir)/lib/byswidget/barima/*.tcl
BYW_BCHILDDETAIL_SCRIPTS = $(srcdir)/lib/byswidget/bchilddetail/*.tcl
BYW_BCOLOR_SCRIPTS = $(srcdir)/lib/byswidget/bcolor/*.tcl
BYW_BCOMBOBOX_SCRIPTS = $(srcdir)/lib/byswidget/bcombobox/*.tcl
BYW_BCOMBOBOXPLUS_SCRIPTS = $(srcdir)/lib/byswidget/bcomboboxplus/*.tcl
BYW_BDIALOG_SCRIPTS = $(srcdir)/lib/byswidget/bdialog/*.tcl
BYW_BEDITMASTER_SCRIPTS = $(srcdir)/lib/byswidget/beditmaster/*.tcl
BYW_BEDITSELECTOR_SCRIPTS = $(srcdir)/lib/byswidget/beditselector/*.tcl
BYW_BENTRYDATE_SCRIPTS = $(srcdir)/lib/byswidget/bentrydate/*.tcl
BYW_BFIELDSDIALOG_SCRIPTS = $(srcdir)/lib/byswidget/bfieldsdialog/*.tcl
BYW_BFILTER_SCRIPTS = $(srcdir)/lib/byswidget/bfilter/*.tcl
BYW_BFONT_SCRIPTS = $(srcdir)/lib/byswidget/bfont/*.tcl
BYW_BFUNCTIONS_SCRIPTS = $(srcdir)/lib/byswidget/bfunctions/*.tcl
BYW_BGUIFUNCTION_SCRIPTS = $(srcdir)/lib/byswidget/bguifunction/*.tcl
BYW_BLISTBOX_SCRIPTS = $(srcdir)/lib/byswidget/blistbox/*.tcl
BYW_BLISTBOXPLUS_SCRIPTS = $(srcdir)/lib/byswidget/blistboxplus/*.tcl
BYW_BLISTORDER_SCRIPTS = $(srcdir)/lib/byswidget/blistorder/*.tcl
BYW_BMULTISELECT_SCRIPTS = $(srcdir)/lib/byswidget/bmultiselect/*.tcl
BYW_BODBC_SCRIPTS = $(srcdir)/lib/byswidget/bodbc/*.tcl
BYW_BSELECTFIELDS_SCRIPTS = $(srcdir)/lib/byswidget/bselectfields/*.tcl
BYW_BSERSEL_SCRIPTS = $(srcdir)/lib/byswidget/bsersel/*.tcl
BYW_BTAB_SCRIPTS = $(srcdir)/lib/byswidget/btab/*.tcl
BYW_BTABLE_SCRIPTS = $(srcdir)/lib/byswidget/btable/*.tcl
BYW_BTOPLEVEL_SCRIPTS = $(srcdir)/lib/byswidget/btoplevel/*.tcl
BYW_BTREEVIEW_SCRIPTS = $(srcdir)/lib/byswidget/btreeview/*.tcl
BYW_BTREEVIEWPLUS_SCRIPTS = $(srcdir)/lib/byswidget/btreeviewplus/*.tcl
BYW_BYSCOMMON_SCRIPTS = $(srcdir)/lib/byswidget/byscommon/*.tcl
BYW_FMENU_SCRIPTS = $(srcdir)/lib/byswidget/fmenu/*.tcl
BYW_IMAGES_FILES = $(srcdir)/lib/byswidget/images/*.gif
BYW_MDI_SCRIPTS = $(srcdir)/lib/byswidget/mdi/*.tcl
BYW_MSGS_FILES = $(srcdir)/lib/byswidget/msgs/*.msg

RMTPS_CLIENT_SCRIPTS = $(srcdir)/lib/rmtps_client/*.tcl \
	$(srcdir)/lib/rmtps_client/*.tol

#========================================================================
# Nothing of the variables below this line need to be changed.  Please
# check the TARGETS section below to make sure the make targets are
# correct.
#========================================================================

#========================================================================
# This is the parameterized name of the library that we are building.
#========================================================================

SHELL		= @SHELL@

srcdir		= @srcdir@
prefix		= @prefix@
exec_prefix	= @exec_prefix@

bindir		= @bindir@
libdir		= @libdir@
datadir		= @datadir@
datarootdir	= @datarootdir@
mandir		= @mandir@
includedir	= @includedir@

DESTDIR		=

PKG_DIR		= $(PACKAGE)$(VERSION)
pkgdatadir	= $(datadir)/$(PKG_DIR)
pkglibdir	= $(libdir)/$(PKG_DIR)
pkgincludedir	= $(includedir)/$(PKG_DIR)

top_builddir	= .

INSTALL		= @INSTALL@
INSTALL_PROGRAM	= @INSTALL_PROGRAM@
INSTALL_DATA	= @INSTALL_DATA@
INSTALL_SCRIPT	= @INSTALL_SCRIPT@

PACKAGE		= @PACKAGE@
VERSION		= @VERSION@
CLEANFILES	= @CLEANFILES@
EXEEXT		= @EXEEXT@
TCL_DEFS	= @TCL_DEFS@
TCL_BIN_DIR	= @TCL_BIN_DIR@
TCL_SRC_DIR	= @TCL_SRC_DIR@
# This is necessary for packages that use private Tcl headers
#TCL_TOP_DIR_NATIVE	= @TCL_TOP_DIR_NATIVE@
# Not used, but retained for reference of what libs Tcl required
TCL_LIBS	= @TCL_LIBS@

#========================================================================
# TCLLIBPATH seeds the auto_path in Tcl's init.tcl so we can test our
# package without installing.  The other environment variables allow us
# to test against an uninstalled Tcl.  Add special env vars that you
# require for testing here (like TCLX_LIBRARY).
#========================================================================

EXTRA_PATH	= $(top_builddir):$(TCL_BIN_DIR)
TCLSH_ENV	= TCL_LIBRARY=`@CYGPATH@ $(TCL_SRC_DIR)/library` \
		  LD_LIBRARY_PATH="$(EXTRA_PATH):$(LD_LIBRARY_PATH)" \
		  LIBPATH="$(EXTRA_PATH):${LIBPATH}" \
		  SHLIB_PATH="$(EXTRA_PATH):${SHLIB_PATH}" \
		  PATH="$(EXTRA_PATH):$(PATH)" \
		  TCLLIBPATH="$(top_builddir)"
TCLSH_PROG	= @TCLSH_PROG@
TCLSH		= $(TCLSH_ENV) $(TCLSH_PROG)

CONFIG_CLEAN_FILES = Makefile

#========================================================================
# Start of user-definable TARGETS section
#========================================================================

#========================================================================
# TEA TARGETS.  Please note that the "libraries:" target refers to platform
# independant files, and the "binaries:" target inclues executable programs and
# platform-dependant libraries.  Modify these targets so that they install
# the various pieces of your package.  The make and install rules
# for the BINARIES that you specified above have already been done.
#========================================================================

all: binaries install-binaries install-lib-binaries

#========================================================================
# The binaries target builds executable programs, Windows .dll's, unix
# shared/static libraries, and any other platform-dependant files.
# The list of targets to build for "binaries:" is specified at the top
# of the Makefile, in the "BINARIES" variable.
#========================================================================

#binaries: pkgIndex

binaries: tolbase

install: all

install-binaries: install-lib-binaries
	$(INSTALL_DATA) pkgIndex.tcl $(DESTDIR)$(pkglibdir);

#========================================================================
# Create the pkgIndex.tcl file.
# It is usually easiest to let Tcl do this for you with pkg_mkIndex, but
# you may find that you need to customize the package.  If so, either
# modify the -hand version, or create a pkgIndex.tcl.in file and have
# the configure script output the pkgIndex.tcl by editing configure.in.
#========================================================================

pkgIndex:
#	sed -e 's/%VERSION%/$(VERSION)/' $(srcdir)/pkgIndex.tcl.in > pkgIndex.tcl

tolbase:
	(echo 'wish $(DESTDIR)$(pkglibdir)/toltk/init_toltk.tcl $$*') > lib/tolbase.sh
	chmod +x lib/tolbase.sh

#========================================================================
# End of user-definable section
#========================================================================

#========================================================================
# Don't modify the file to clean here.  Instead, set the "CLEANFILES"
# variable in configure.in
#========================================================================

distclean: clean
	-rm -f $(CONFIG_CLEAN_FILES)
	-rm -f config.cache config.log config.status

#========================================================================
# Install binary object libraries.  On Windows this includes both .dll and
# .lib files.  Because the .lib files are not explicitly listed anywhere,
# we need to deduce their existence from the .dll file of the same name.
# Additionally, the .dll files go into the bin directory, but the .lib
# files go into the lib directory.  On Unix platforms, all library files
# go into the lib directory.  In addition, this will generate the pkgIndex.tcl
# file in the install location (assuming it can find a usable tclsh8.2 shell)
#
# You should not have to modify this target.
#========================================================================

install-lib-binaries:
	@mkdir -p $(DESTDIR)$(pkglibdir)
#	@mkdir -p $(DESTDIR)$(pkglibdir)/doc
#	@mkdir -p $(DESTDIR)$(pkglibdir)/samples
	@mkdir -p $(DESTDIR)$(pkglibdir)/toltk
	@mkdir -p $(DESTDIR)$(pkglibdir)/toltk/images
	@mkdir -p $(DESTDIR)$(pkglibdir)/toltk/msgs
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/barima
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bchilddetail
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bcolor
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bcombobox
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bcomboboxplus
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bdialog
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/beditmaster
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/beditselector
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bentrydate
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bfieldsdialog
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bfilter
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bfont
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bfunctions
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bguifunction
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/blistbox
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/blistboxplus
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/blistorder
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bmultiselect
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bodbc
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bselectfields
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/bsersel
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/btab
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/btable
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/btoplevel
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/btreeview
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/btreeviewplus
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/byscommon
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/fmenu
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/images
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/mdi
	@mkdir -p $(DESTDIR)$(pkglibdir)/byswidget/msgs
	@mkdir -p $(DESTDIR)$(pkglibdir)/rmtps_client
	@echo "Installing Tolbase in $(DESTDIR)$(pkglibdir)"
#	@echo "Installing documentation files in $(DESTDIR)$(pkglibdir)/doc"
#	@for i in $(DOC_FILES) ; do \
#	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/doc ; \
#	done;
#	@echo "Installing sample files in $(DESTDIR)$(pkglibdir)/samples"
#	@for i in $(SAMPLE_FILES) ; do \
#	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/samples ; \
#	done;
	@echo "Installing TolTk script files in $(DESTDIR)$(pkglibdir)/toltk"
	@for i in $(TTK_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/toltk ; \
	done;
	@chmod o+rw $(DESTDIR)$(pkglibdir)/toltk/project.ini
	@echo "Installing TolTk language files in $(DESTDIR)$(pkglibdir)/toltk/msgs"
	@for i in $(TTK_LANG_FILES) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/toltk/msgs ; \
	done;
	@echo "Installing Toltk image files in $(DESTDIR)$(pkglibdir)/toltk/images"
	@for i in $(TTK_IMAGE_FILES) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/toltk/images ; \
	done;
	@echo "Installing BysWidget script files in $(DESTDIR)$(pkglibdir)/byswidget"
	@for i in $(BYW_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget ; \
	done;
	@echo "Installing BysWidget bcombobox scripts in $(DESTDIR)$(pkglibdir)/byswidget/bcombobox"
	@for i in $(BYW_BCOMBOBOX_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bcombobox ; \
	done;
	@echo "Installing BysWidget bcomboboxplus scripts in $(DESTDIR)$(pkglibdir)/byswidget/bcomboboxplus"
	@for i in $(BYW_BCOMBOBOXPLUS_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bcomboboxplus ; \
	done;
	@echo "Installing BysWidget beditmaster scripts in $(DESTDIR)$(pkglibdir)/byswidget/beditmaster"
	@for i in $(BYW_BEDITMASTER_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/beditmaster ; \
	done;
	@echo "Installing BysWidget beditselector scripts in $(DESTDIR)$(pkglibdir)/byswidget/beditselector"
	@for i in $(BYW_BEDITSELECTOR_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/beditselector ; \
	done;
	@echo "Installing BysWidget bentrydate scripts in $(DESTDIR)$(pkglibdir)/byswidget/bentrydate"
	@for i in $(BYW_BENTRYDATE_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bentrydate ; \
	done;
	@echo "Installing BysWidget bfieldsdialog scripts in $(DESTDIR)$(pkglibdir)/byswidget/bfieldsdialog"
	@for i in $(BYW_BFIELDSDIALOG_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bfieldsdialog ; \
	done;
	@echo "Installing BysWidget bfilter scripts in $(DESTDIR)$(pkglibdir)/byswidget/bfilter"
	@for i in $(BYW_BFILTER_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bfilter ; \
	done;
	@echo "Installing BysWidget bfunctions scripts in $(DESTDIR)$(pkglibdir)/byswidget/bfunctions"
	@for i in $(BYW_BFUNCTIONS_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bfunctions ; \
	done;
	@echo "Installing BysWidget bguifunction scripts in $(DESTDIR)$(pkglibdir)/byswidget/bguifunction"
	@for i in $(BYW_BGUIFUNCTION_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bguifunction ; \
	done;
	@echo "Installing BysWidget blistbox scripts in $(DESTDIR)$(pkglibdir)/byswidget/blistbox"
	@for i in $(BYW_BLISTBOX_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/blistbox ; \
	done;
	@echo "Installing BysWidget blistboxplus scripts in $(DESTDIR)$(pkglibdir)/byswidget/blistboxplus"
	@for i in $(BYW_BLISTBOXPLUS_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/blistboxplus ; \
	done;
	@echo "Installing BysWidget bmultiselect scripts in $(DESTDIR)$(pkglibdir)/byswidget/bmultiselect"
	@for i in $(BYW_BMULTISELECT_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bmultiselect ; \
	done;
	@echo "Installing BysWidget bodbc scripts in $(DESTDIR)$(pkglibdir)/byswidget/bodbc"
	@for i in $(BYW_BODBC_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bodbc ; \
	done;
	@echo "Installing BysWidget bselectfields scripts in $(DESTDIR)$(pkglibdir)/byswidget/bselectfields"
	@for i in $(BYW_BSELECTFIELDS_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bselectfields ; \
	done;
	@echo "Installing BysWidget bsersel scripts in $(DESTDIR)$(pkglibdir)/byswidget/bsersel"
	@for i in $(BYW_BSERSEL_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bsersel ; \
	done;
	@echo "Installing BysWidget blistorder scripts in $(DESTDIR)$(pkglibdir)/byswidget/blistorder"
	@for i in $(BYW_BLISTORDER_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/blistorder ; \
	done;
	@echo "Installing BysWidget bfont scripts in $(DESTDIR)$(pkglibdir)/byswidget/bfont"
	@for i in $(BYW_BFONT_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bfont ; \
	done;
	@echo "Installing BysWidget bcolor scripts in $(DESTDIR)$(pkglibdir)/byswidget/bcolor"
	@for i in $(BYW_BCOLOR_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bcolor ; \
	done;
	@echo "Installing BysWidget btable scripts in $(DESTDIR)$(pkglibdir)/byswidget/btable"
	@for i in $(BYW_BTABLE_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/btable ; \
	done;
	@echo "Installing BysWidget btab scripts in $(DESTDIR)$(pkglibdir)/byswidget/btab"
	@for i in $(BYW_BTAB_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/btab ; \
	done;
	@echo "Installing BysWidget bdialog scripts in $(DESTDIR)$(pkglibdir)/byswidget/bdialog"
	@for i in $(BYW_BDIALOG_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/bdialog ; \
	done;
	@echo "Installing BysWidget barima scripts in $(DESTDIR)$(pkglibdir)/byswidget/barima"
	@for i in $(BYW_BARIMA_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/barima ; \
	done;
	@echo "Installing BysWidget btoplevel scripts in $(DESTDIR)$(pkglibdir)/byswidget/btoplevel"
	@for i in $(BYW_BTOPLEVEL_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/btoplevel ; \
	done;
	@echo "Installing BysWidget btreeview scripts in $(DESTDIR)$(pkglibdir)/byswidget/btreeview"
	@for i in $(BYW_BTREEVIEW_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/btreeview ; \
	done;
	@echo "Installing BysWidget btreeview scripts in $(DESTDIR)$(pkglibdir)/byswidget/btreeview"
	@for i in $(BYW_BTREEVIEWPLUS_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/btreeviewplus ; \
	done;
	@echo "Installing BysWidget byscommon scripts in $(DESTDIR)$(pkglibdir)/byswidget/byscommon"
	@for i in $(BYW_BYSCOMMON_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/byscommon ; \
	done;
	@echo "Installing BysWidget fmenu scripts in $(DESTDIR)$(pkglibdir)/byswidget/fmenu"
	@for i in $(BYW_FMENU_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/fmenu ; \
	done;
	@echo "Installing BysWidget image files in $(DESTDIR)$(pkglibdir)/byswidget/images"
	@for i in $(BYW_IMAGES_FILES) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/images ; \
	done;
	@echo "Installing BysWidget mdi scripts in $(DESTDIR)$(pkglibdir)/byswidget/mdi"
	@for i in $(BYW_MDI_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/mdi ; \
	done;
	@echo "Installing BysWidget msgs files in $(DESTDIR)$(pkglibdir)/byswidget/msgs"
	@for i in $(BYW_MSGS_FILES) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/byswidget/msgs ; \
	done;
	@echo "Installing RMTPS_client script files in $(DESTDIR)$(pkglibdir)/rmtps_client"
	@for i in $(RMTPS_CLIENT_SCRIPTS) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir)/rmtps_client ; \
	done;
	@echo "Installing dist info files in $(DESTDIR)$(pkglibdir)"
	@for i in $(DIST_FILES) ; do \
	    $(INSTALL_SCRIPT) $$i $(DESTDIR)$(pkglibdir) ; \
	done;

#========================================================================
# Install binary executables (e.g. .exe files)
#
# You should not have to modify this target.
#========================================================================

Makefile: $(srcdir)/Makefile.in  $(top_builddir)/config.status
	cd $(top_builddir) \
	  && CONFIG_FILES=$@ CONFIG_HEADERS= $(SHELL) ./config.status

uninstall-binaries:
	list='$(lib_BINARIES)'; for p in $$list; do \
	  rm -f $(DESTDIR)$(pkglibdir)/$$p; \
	done
	list='$(RUNTIME_SOURCES)'; for p in $$list; do \
	  rm -f $(DESTDIR)$(pkglibdir)/$$p; \
	done
	list='$(bin_BINARIES)'; for p in $$list; do \
	  rm -f $(DESTDIR)$(bindir)/$$p; \
	done

.PHONY: all binaries clean depend distclean doc install libraries test

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
